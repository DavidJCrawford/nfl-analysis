/** What the site covers.
 *
 *  One switch, as in F1. nflverse carries play-by-play back to 1999 and the
 *  format is uniform, so widening is a matter of emitting more seasons and
 *  turning this into a list — not of changing any page.
 *
 *  SPEC §2. */
export const SEASON = 2026;

/** The regular season is 18 weeks. Kept here rather than derived, so a page can
 *  lay out the full grid before any of it has been played. */
export const WEEKS = 18;
